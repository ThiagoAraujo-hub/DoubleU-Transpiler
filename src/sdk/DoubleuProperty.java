package sdk;

import sdk.enums.Scopes;

public class DoubleuProperty extends DoubleuVariable {

    public DoubleuProperty(String type, String name) {
        super(type, name, Scopes.OBJECT);
    }
}
